public class Magic {

    /**
     * Diese Klasse soll encrypten und decrypten.. bereits mit Int getestet
     */


   /* public void zaehleBisZahlundRufe(int zahl) {

        BigInteger q = BigInteger.valueOf(13);                          // Basis
        BigInteger e = BigInteger.valueOf(342);                            // exponent
        BigInteger m = BigInteger.valueOf(899);                            // m = (q*p)   evt auch n nennen.


        while (e.compareTo(BigInteger.valueOf(0)) >0) {                      // solange wie e kleiner ist als 0

            if (e.mod(BigInteger.valueOf(2)).equals(BigInteger.valueOf(ZERO))) {                // falls e eine gerade Zahl ist
                // Anweisung
                e % 2 = e1;                 // e modulo 2
                Math.pow(q,2);              // q^2


            } else {

                // Andere Anweisungen
                e - 1 = e2;                 // e minus 1
                (p * q) % n;                // (p * q) modulo m

            }

            // solange bis e = 0
            // dann ist p  das resultat von q^e mod n



            System.out.println (p);       // gebe p aus

        }
        System.out.println ("Modulare Expo:");
    }

    //toDo: Methode für das einlesen und encrypten erstellen/hierherbewegen..


    // toDo: text.txt einlesen und in Array abspeichern


    // toDo: text.txt Array in ASCII umwandeln --> (int)charArray


    // toDo: ASCII-Code einzeln mit schneller Expo Algo verschlüsseln


    // toDo: verschlüselter ASCII-Code einzeln mit Komma getrennt in chiffre.txt speichern
*/

}
